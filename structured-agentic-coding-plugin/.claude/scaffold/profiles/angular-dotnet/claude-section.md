<!--
Profile CLAUDE.md overlay for angular-dotnet.

This file is RESERVED for profile-specific additions to the generated
CLAUDE.md. It is currently empty because the base `_be-section.md` and
`_fe-section.md` fragments already cover the commands and directories
angular-dotnet needs. Future overlay content (e.g., references to the
`/openapi-sync` command, .NET-specific conventions) would land here.

The overlay mechanism itself ships with the skill refactor in Phase 6 —
this file is a forward-declared placeholder so the profile directory
layout is consistent with nestjs-query-be and refine-nestjs-query-fe.
-->
