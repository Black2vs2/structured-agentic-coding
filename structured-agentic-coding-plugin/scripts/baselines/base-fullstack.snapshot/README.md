# Base Fullstack Fixture

Empty target project used by `smoke-test.sh` to scaffold profile=`base`, SCOPE=`fullstack`. Placeholder values are passed as scaffold-script CLI args; this directory only needs to exist so scaffold.sh has a target to write into.
