# NestJS-Query BE Fixture

Empty target project used by `smoke-test.sh` to scaffold profile=`nestjs-query-be`, SCOPE=`be`. Placeholder values are passed as scaffold-script CLI args; this directory only needs to exist so scaffold.sh has a target to write into. Represents a standalone NestJS backend repo (no frontend).
