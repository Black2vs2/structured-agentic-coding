# Refine NestJS-Query FE Fixture

Empty target project used by `smoke-test.sh` to scaffold profile=`refine-nestjs-query-fe`, SCOPE=`fe`. Placeholder values are passed as scaffold-script CLI args; this directory only needs to exist so scaffold.sh has a target to write into. Represents a standalone React + Vite + Refine frontend repo (no backend).
